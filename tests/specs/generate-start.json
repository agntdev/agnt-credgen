[
  {
    "name": "Generate Batch",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Initiate credential generation workflow"
            }
          }
        ],
        "send": {
          "callback": "generate:start"
        }
      }
    ]
  }
]
